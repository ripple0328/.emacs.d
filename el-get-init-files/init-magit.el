(global-set-key (kbd "C-c m") 'magit-status)

;; magit-emacsclient-executable looks emacsclient in /usr/bin/emacsclient that was
;; not related to emacs for osx, so I customize it
;; (setq magit-emacsclient-executable (concat invocation-directory "bin/emacsclient"))
